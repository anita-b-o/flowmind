# Docker

Local infrastructure is defined in the root `docker-compose.yml`.

Application Dockerfiles are intentionally deferred until the first deploy milestone, after app startup commands stabilize.
